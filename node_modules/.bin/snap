#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_snap@1.1.0@snap/snap.js" "$@"
  ret=$?
else 
  node  "$basedir/../_snap@1.1.0@snap/snap.js" "$@"
  ret=$?
fi
exit $ret
